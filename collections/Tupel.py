#Tuple has differences from Lists
#Tuple is IMMUTABLE (once created, it cannot be changed)
coordinates = (4, 5)
list_of_tuples = [(4, 5), (6, 7), (80, 34)]
print(coordinates[0], coordinates[1])

#INVALID OPERATION
#   coordinates[0] = 3